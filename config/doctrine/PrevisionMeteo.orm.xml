<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Domaine\Entite\PrevisionMeteo" table="prevision_meteo">
        <unique-constraints>
            <!-- Une prévision par journée : la seconde saisie révise la première. -->
            <unique-constraint name="uniq_prevision_meteo_date" columns="date_prevision"/>
        </unique-constraints>

        <id name="id" type="integer" column="id">
            <generator strategy="IDENTITY"/>
        </id>

        <field name="date" type="date_immutable" column="date_prevision" nullable="false"/>
        <field name="texte" type="string" column="texte" length="255" nullable="false"/>
    </entity>
</doctrine-mapping>

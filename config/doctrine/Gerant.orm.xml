<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Domaine\Entite\Gerant" table="gerant">
        <unique-constraints>
            <unique-constraint name="uniq_gerant_email" columns="email"/>
        </unique-constraints>

        <id name="id" type="integer" column="id">
            <generator strategy="IDENTITY"/>
        </id>

        <field name="email" type="string" column="email" length="180" nullable="false"/>
        <!-- Un condensat, jamais le mot de passe en clair. -->
        <field name="motDePasse" type="string" column="mot_de_passe" length="255" nullable="false"/>
    </entity>
</doctrine-mapping>
